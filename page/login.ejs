<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h2> 로그인 페이지 </h2>

    <!-- 데이터를 보내, 데이터를 주면서 요청해야 함, 그래서 post 방식 도 사용 -->
    <form action="/login" method="post">
        <label for=""> 아이디 </label>
        <input type="text" name="user_id">
    
        <label for=""> 비밀번호 </label>
        <input type="text" name="user_pw">
        <!-- 이 모양을 보면 
            {
                user_id : name user_id 를 가지고 있는 input value, 
                user_pw : name 속성에 user_pw 를 갖고 있는 input value, 
            }
        -->
        <!-- 
            {
                user_id : 1, 
                user_pw : 2
            }
            이걸 POST 방식으로 보내면 > INDEX.JS 로 온다. 
         -->

    </form>
        <!-- action="/login" 이걸 지정 안 하면, home 에서 보낸다❓❓❓❓❓❓❓❓❓❓ -->
        <!-- 가긴 가는데, key 값을 모르네? > 그게 name 으로 전달된다. -->
    
    <button>로그인</button>


</body>
</html>