<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>


<h2>리스트 리스트 🤟</h2>

<a href="/insert"> 추가 페이지로 이동 </a>

<table> 
    <tr>
        <th> <a href="/delete/<%= el.id %>"  </a> 삭제</th>
        <th>수정</th>
        <th>아이디</th>
        <th>이름</th>
        <th>전화번호</th>
        <th>시리즈</th>
    </tr>

    <% if(data) {
        
        <% data.forEach( (el, index) => { %>
            <tr>
                <td> <a href="/delete/<%= el.id %"> 삭제 </a>  </td>
                <td> <a href="/edit/<%= el.id %>">수정</a> </td>
                <td><%= el.id %></td>
                <td><%= el.name &></td>
                <td><%= el.number %> </td>
                <td><%= el.series %> </td>
            </tr>

        <% }); %>
        
        %>

    <% } else { %>
    <% } %>
        <!-- <% 다음 space bar 필수 ⭐⭐⭐ -->



    <!-- 데이터 베이스로 요청해서 data 객체로 전달해주면, 
        우리가, products 데이터 베이스에 등록해놓은 값, 이 
        '배열' 로 들어오고 
        데이터 베이스 안에 있는 키 (id, ) 
        
        data = [{id : 4, name : sadafa, number : asdfsafsd, series : asdfawef}, 
        {id : 6, name : sada123fa, number : a123sdfsafsd, series : a123sdfawef}, 
        ] 

        한줄이 하나의 객체, 
        밑에 있는 건 두 번째 객체, 

        이걸 foreach 로 반복을 돌리면, 첫 번째에 id 4 


    
    -->

</table>

</body>

<script>



    // 문법 쓰기 위한 용도
    // data.forEach( (el, index) => {
        
    // });


    // ejs 문법 용도
    // if(data) {

    // } else {

    // }

</script>



</html>